# T-013: v2.1·v2.2 폐기 잔여물 grep 정리

| 항목 | 내용 |
|---|---|
| **Phase** | 1-2 |
| **우선순위** | Med |
| **예상 소요** | 1일 |
| **의존성** | T-010 |
| **관련 기능 ID** | — (폐기 정리) |
| **PRD 참조** | 변경 요약 v2.0→v2.1·v2.1→v2.2 |

## 산출물

- grep 보고서 (텍스트 파일 또는 PR 본문)
- 발견된 잔여물 모두 제거된 코드베이스

## 검증 기준

- 다음 키워드 grep 결과 **0건**:
  - `公認`
  - `公式LINEに参加`
  - `LINE卒業`
  - `F022` `F023` `F024` `F025` (주석에서도 제거)
  - `circle_faqs` (테이블·코드)
  - `先輩のコメント`

## 세부 작업

- [ ] `rg "公認" --type ts --type tsx --type md` 0건
- [ ] `rg "公式LINEに参加" --type ts --type tsx` 0건
- [ ] `rg "LINE卒業|LINEはもう古い"` 0건
- [ ] `rg "circle_faqs"` 0건 (마이그레이션·쿼리·UI)
- [ ] `rg "先輩のコメント|先輩コメント"` 0건
- [ ] PRD 변경 요약 표 (v2.0→v2.1·v2.1→v2.2) 의 「삭제 항목」 코드에서 흔적 추가 점검
- [ ] 단체 명칭은 `サークル・部活動` 통일 [[avoid-koujin-wording]]

## 위험·주의사항

- ⚠️ **마이그레이션 폴더 안 잔여물** — 「Phase 0 에서 만든 `circle_faqs` 테이블 마이그레이션」이 있을 수 있음. **롤백 마이그레이션** (DROP TABLE IF EXISTS) 추가 권장.
- ⚠️ **주석에 남은 폐기 ID** — `// TODO: F022 아카이브` 같은 주석. grep 결과 0건까지 제거.

## 테스트 체크리스트

- [ ] grep 6종 모두 0건 확인 (보고서로 남김)
- [ ] Supabase `list_tables` 에 `circle_faqs` 없음

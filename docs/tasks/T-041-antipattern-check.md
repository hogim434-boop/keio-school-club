# T-041: 안티패턴 5종 grep 점검

| 항목 | 내용 |
|---|---|
| **Phase** | 1-6 |
| **우선순위** | High |
| **예상 소요** | 1일 |
| **의존성** | Phase 1-5 종료 |
| **관련 기능 ID** | A-1·A-2·A-3·A-4·A-5 |
| **PRD 참조** | PRD 12-1 안티패턴 |

## 산출물

- 안티패턴 점검 보고서

## 검증 기준

| 코드 | 점검 |
|---|---|
| **A-1** | DM 발신·気になる 클릭 시 미로그인 → `redirect_to` 보존되어 자동 복귀 |
| **A-2** | LINE 대체 메시지 (`LINE 卒業` 등) 0건 (T-040 보강) |
| **A-3** | `show_profile` 마이그레이션 DEFAULT false + UI 토글 OFF 기본 |
| **A-4** | 별점·평점 코드·UI 0개 |
| **A-5** | DM 既読 표시 0개 — 「未読 N件」 만 |

## 세부 작업

- [ ] A-1 — 인증 필요 행위 진입 시 redirect_to 보존 — 코드 검색 + 수동 테스트
- [ ] A-2 — T-040 grep 보고서 인용
- [ ] A-3 — `profiles.show_profile` SQL 조회 + UI 스크린샷
- [ ] A-4 — `rg "rating|星|★|☆"` 결과 0건
- [ ] A-5 — `rg "既読|read_at|read_by"` — `is_read_by_recipient` (배지용) 만 허용, 그 외 표시 코드 0건

## 위험·주의사항

- ⚠️ **「未読 N件」 vs 「N人が既読」 차이** — 후자는 금지. 단순 카운트 배지만 허용.
- ⚠️ **베타 후 사용자가 既読 요청** — Phase 1 에서 추가 금지. 추후 검토.

## 테스트 체크리스트

- [ ] 5종 모두 통과 (체크박스 5개 모두 ✓)

# T-019: 캘린더 페이지 (月表示 + リスト 토글)

| 항목 | 내용 |
|---|---|
| **Phase** | 1-3 |
| **우선순위** | High |
| **예상 소요** | 3일 |
| **의존성** | T-014 |
| **관련 기능 ID** | F036·F037·F038 |
| **PRD 참조** | PRD 5-4 F036·F037·F038 · 9-2 react-day-picker |

## 산출물

- `app/(tabs)/calendar/page.tsx`
- 月表示 (shadcn `Calendar` 기반)
- リスト 토글

## 검증 기준

- 月表示 → 카테고리 컬러 도트 (최대 3개, 초과 시 `+n`)
- 날짜 클릭 → 하단 Sheet 에 해당 날 이벤트
- リスト 토글 → 다가오는 이벤트 시간순, 날짜 sticky 헤더
- 비로그인 열람 가능

## 세부 작업

- [ ] shadcn `Calendar` 추가
- [ ] 月 그리드 + 카테고리 컬러 도트 (modifiers)
- [ ] 날짜 클릭 → `Sheet` 표시
- [ ] 토글 버튼 (月 / リスト)
- [ ] リスト 뷰 — 날짜 sticky 헤더 + 카테고리 배지
- [ ] 공개 캐싱 `tags:["events:public"]`

## 위험·주의사항

- ⚠️ **dnycp 한국어/일본어 로케일** — `react-day-picker` 의 `locale` prop 에 `ja` 적용.
- ⚠️ **공개 캐싱과 정합** — 이벤트 수정 시 (T-021) `revalidateTag` 호출 필수.
- ⚠️ **`F042` 글로벌 캘린더** 는 Phase 2. Phase 1 은 전체 동아리 이벤트 통합 노출.

## 테스트 체크리스트

- [ ] 비로그인 진입 → 정상 표시
- [ ] 토글 → 뷰 전환
- [ ] 날짜 클릭 → Sheet 이벤트 표시
